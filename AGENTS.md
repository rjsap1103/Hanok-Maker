# Hanok Maker 개발 가이드라인 및 수정 금지 원칙 (Strict Rules)

이 문서는 `Hanok Maker` 프로젝트의 기능 추가 및 코드 수정 시 반드시 준수해야 하는 최우선 개발 원칙과 절대 금지 사항을 정의합니다.

---

## 1. 절대 수정 금지 항목 (Never Touch / Preserved Modules)

1. **STEP 01 토대/기단 시스템 보존**:
   - `Step1FoundationManager.tsx` 및 주춧돌 스냅/배치/Ghost Preview 확정 로직은 이미 정상 동작 중이므로 임의로 변경하지 않습니다.
2. **STEP 02 목구조 결구 및 방향 계산 로직 보존**:
   - 기둥(Pillar) 세우기 및 대들보(Beam) 두 기둥 간 주두 연결, 방향 벡터 계산 로직은 절대 훼손하지 않습니다.
3. **바닥(Floor) 자동 생성 및 재질 커스텀 기능 유지**:
   - `HanokFloorManager.tsx`에서 주춧돌 기반으로 바닥이 자동 연산되는 로직과, 바닥 클릭 시 `ComponentInfo`에서 재질(마루/온돌/흙바닥)을 즉시 교체하는 기능은 어떤 수정 작업 중에도 100% 정상 유지되어야 합니다.
4. **상태 스토어 인터페이스 호환성 보장**:
   - `useBuildStore` 내 `placedParts` 배열의 기존 필드명(`id`, `partId`, `name`, `category`, `position`, `rotation`, `scale`)은 절대 삭제하거나 변경하지 않습니다. 새 필드가 필요한 경우 선택적(`?`) 속성으로 추가만 허용합니다.

---

## 2. 한옥 건축 가구식 위계 원칙 (Architectural Hierarchy)

1. **외곽 입면 일치 (Alignment)**:
   - **외곽 벽체 선 = 기둥 중심선 = 상부 박공벽(합각벽) 기준선**이 수직으로 오차 없이 일치해야 합니다.
   - 박공벽은 지붕 처마 끝이 아닌, 실제 배치된 최외곽 기둥의 좌표(`minX`, `maxX`) 위에 위치해야 합니다.
2. **처마 내밀기 (Eaves Overhang)**:
   - 지붕과 서까래는 비바람을 차단하는 전통 건축 원리에 따라, 벽체 및 박공벽 선 바깥으로 최소 0.6m~0.9m 이상 시원하게 돌출되어야 합니다.
3. **내부 프레임 결구 (사개맞춤 및 간섭 방지)**:
   - 대들보(Beam) 등 내부 수평 골조는 지붕 서까래를 뚫고 솟아오르지 않도록 기둥 머리(사개맞춤) 높이 이하에 정밀 안착되어야 합니다.
   - 벽체와 창호가 프레임의 최외곽을 마감하고, 가구 골조는 벽체/지붕 안쪽으로 유기적으로 감싸져야 합니다.

---

## 3. 검증 필수 체크리스트 (Verification Checklist)

코드 수정 후에는 반드시 다음 사항을 단계별로 검증해야 합니다:
- [ ] **빌드 무결성**: `npm run build` 실행 시 TypeScript 에러 0건 확인.
- [ ] **개구부 호환성**: 벽체 위치나 지붕이 변경되어도 창호(격자창)와 문(미닫이문, 전통판문)의 위치가 틀어지거나 분리되지 않는지 확인.
- [ ] **모듈형 확장성 검증**:
  - 기본 4기둥 구조(1 bay × 1 bay)
  - 확장 6기둥 구조(2 bays × 1 bay) 및 8기둥 구조
  - 위 두 케이스 모두에서 벽체·박공벽·처마 정렬이 깨지지 않는지 브라우저 시각적 검증(Front & Isometric View) 필수 수행.

---

## 4. 코드 설명 및 한글 주석 원칙 (Korean Explanatory Comments)

1. **초보자 친화적 한글 주석 필수**:
   - 모든 컴포넌트, 상태 스토어, 계산식, Three.js 렌더링 로직에는 초보자도 이해하기 쉬운 친절한 한글 주석을 작성합니다.
   - 수학 공식(삼각함수, 벡터 계산, 회전각 등)과 건축학적 치수 결정 배경(처마 내밀기, 안착 높이 등)에는 "왜 이 수식이 필요한지"를 주석으로 반드시 명시합니다.
2. **신규/추가 코드 일관성 유지**:
   - 향후 추가되는 모든 신규 기능 및 수정 코드에도 동일하게 친절한 한글 설명 주석을 적용합니다.


